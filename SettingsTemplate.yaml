body:
  - type: textBlock
    attributes:
      name: description
      description: >
        Please provide a Client ID token and OAuth token (New Twitch API requires a OAuth token).
  - type: input
    attributes:
      name: client_id
      label: 'Client ID:'
      description: Twitch.tv Client ID
  - type: input
    attributes:
      name: oauth_token
      label: 'OAuth Token:'
      description: Twitch.tv OAuth Token