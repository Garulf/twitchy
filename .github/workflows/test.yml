name: "test"
on:
  workflow_dispatch:
jobs:
  deps:
    name: "test"
    runs-on: windows-latest
    steps:
      - name: Delete previous drafts
        uses: Garulf/delete-draft-releases@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

