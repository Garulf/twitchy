name: "test"
on:
  workflow_dispatch:
jobs:
  deps:
    name: "test"
    runs-on: ubuntu-latest
    steps:
      - name: Delete previous drafts
        uses: Garulf/delete-draft-releases@v0.2.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

